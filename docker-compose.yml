version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGODB_URI=${MONGODB_URI}
    volumes:
      - ./uploads:/app/server/uploads
    restart: unless-stopped
